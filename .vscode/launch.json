{
  "version": "0.2.0",
  "configurations": [{
    "name": "Pinewood Derby (Docker Compose)",
    "type": "node-terminal",
    "request": "launch",
    "command": "docker compose up",
    "cwd": "${workspaceFolder}"
  }]
}
